<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <item
        android:id="@+id/action_connect_device"
        android:title="Connect Device"
        android:icon="@drawable/ic_connect"
        app:showAsAction="always"/>
    
    <item
        android:id="@+id/action_disconnect_device"
        android:title="Disconnect Device"
        android:icon="@drawable/ic_disconnect"
        app:showAsAction="always"/>
    
    <item
        android:id="@+id/action_refresh_devices"
        android:title="Refresh Devices"
        android:icon="@drawable/ic_refresh"
        app:showAsAction="ifRoom"/>
</menu>
